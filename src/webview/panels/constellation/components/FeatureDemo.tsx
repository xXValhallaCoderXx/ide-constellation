// (Removed) FeatureDemo component was a development-only showcase for tooltips/toasts.
// Stub left intentionally so any lingering imports fail obviously at runtime rather than bundling large demo UI.
// Safe to delete in a future breaking cleanup.
export {}; 